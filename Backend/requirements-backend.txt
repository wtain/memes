# Runtime deps for the Backend API server.
# Versions are kept in sync with the top-level requirements.txt.
# DO NOT add ML/OCR/batch/GPU libs here — this file is used by CI for
# backend tests and avoids the opencv/paddleocr conflict in requirements.txt.
fastapi==0.128.0
uvicorn==0.40.0
SQLAlchemy==2.0.45
asyncpg==0.31.0
pgvector==0.4.2
pydantic==2.12.5
python-dotenv==1.2.1
python-multipart==0.0.32
greenlet==3.5.3
